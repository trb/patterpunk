services:
  patterpunk:
    build: patterpunk
    restart: unless-stopped
    volumes:
      - ./patterpunk/src:/app
    environment:
      # Flush print statements immediately
      PYTHONUNBUFFERED: 1
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
